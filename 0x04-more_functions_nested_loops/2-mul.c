#include "main.h"

/**
 * mul - multiplies two integers
 * @a: character to be multiplied by b
 * @b: character to be multiplied by a
 * Return: 0
 */
int mul(int a, int b);
{
	int a, b;
	int c = a * b;

	return ("%d", c);
}
